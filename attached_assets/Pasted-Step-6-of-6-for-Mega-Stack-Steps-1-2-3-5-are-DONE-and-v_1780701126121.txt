Step 6 of 6 for Mega Stack. Steps 1, 2, 3, 5 are DONE and verified on
device. (Step 4 lazy-loading is being done after this.) Storage layer in
megaStackStorage.js is complete: updateLastPage(id, pageIndex) exists and
was verified in the Step 1 test, and every saved stack already has a
lastPage field (currently always 0).

GOAL: reading progress for Mega Stack. When the user reads a saved Mega
Stack and swipes between pages, remember the page. When they reopen that
stack later, open it on the page they left off.

HARD RULES (my project guidelines — follow exactly):
- NO database. Phone-local only, via megaStackStorage.js. Only CALL
  updateLastPage — add no new storage logic.
- CLEAN, ORGANISED CODE, no spaghetti. Keep changes minimal and contained.
  This is wiring, not new architecture.
- This must apply to MEGA STACK saved stacks ONLY. Do NOT add progress
  saving to Quick Stack (throwaway) or the PDF reader. Quick Stack and PDF
  must behave exactly as now.
- Do NOT change the PagedReader's own behaviour for other callers. The
  reader is shared — be careful not to make it save progress for Quick
  Stack/PDF.
- No paywall / locking (9.7).

WHAT TO BUILD:
1. SAVE progress: when the user is reading an OPENED MEGA STACK and changes
   page (swipe or page-jump), persist the current page index by calling
   updateLastPage(stackId, pageIndex).
   - Only when the open stack is a saved Mega Stack (we know its stackId).
   - Keep it cheap: debounce or save on page-change settle — don't thrash
     the disk on every animation frame. Saving once per landed page is
     fine.
   - The reader must know WHICH stack id is open. Pass the stackId through
     when opening a saved stack from the library (handleOpenStack already
     knows the id). Do this cleanly — a prop / context value, not a global
     hack.

2. RESTORE progress: when opening a saved Mega Stack from the library,
   start the PagedReader on that stack's saved lastPage (instead of always
   page 0). If lastPage is out of range for any reason, clamp to a valid
   page (0..pageCount-1). Never crash on a bad value.

3. Quick Stack and PDF: unchanged. They open at page 0, save no progress.
   Confirm the mechanism is gated on "this is a saved Mega Stack with an
   id" so the other two paths are untouched.

4. No silent failures (Lesson 2): if updateLastPage throws, fail quietly in
   the background (a console.error is fine — do NOT interrupt reading with
   an error popup for a progress-save miss). Reading must never break
   because a progress save failed.

TEST PLAN (tell me how to verify on the phone):
- Open a saved Mega Stack with several pages, swipe to e.g. page 3, leave
  the reader.
- Reopen the SAME stack from the library → it opens on page 3.
- Close and reopen the whole APP, open that stack → still opens on page 3
  (proves it persisted to disk).
- Open a DIFFERENT saved stack → opens on ITS own last page (progress is
  per-stack, via lastPage in its index entry).
- Quick Stack: capture, read, swipe, exit, re-capture → still starts at
  page 0, no progress (unchanged).
- PDF: open, swipe, exit, reopen → starts at page 0 (unchanged).

When done, tell me:
- Files changed (confirm storage stayed in megaStackStorage.js).
- How the stackId reaches the reader (the clean mechanism, not a global).
- Confirm Quick Stack + PDF are untouched (progress gated to saved Mega
  Stacks only).
- How to test on the phone.
- Anything risky flagged.